# m h dom mon dow user command
0 3 * * * root sudo /usr/bin/systemctl restart app

# Missing user field (should be flagged)
30 3 * * * /usr/local/bin/report-metrics >/var/log/metrics.log 2>&1

# Env applies to jobs that follow
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin
45 2 * * * app /usr/local/bin/job-a >/var/log/a.log 2>&1
